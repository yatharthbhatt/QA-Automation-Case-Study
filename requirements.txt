# Core test stack
pytest==8.2.0
playwright==1.44.0
pytest-playwright==0.5.0
requests==2.32.3

# Parallel execution
pytest-xdist==3.6.1

# Reporting
pytest-html==4.1.1

# Retries for genuinely non-deterministic externalities (use sparingly — see TESTING_APPROACH.md)
pytest-rerunfailures==14.0

# Config & data
python-dotenv==1.0.1
PyYAML==6.0.1

# HTTP retry/backoff helper
tenacity==8.3.0

# Deterministic 2FA/TOTP codes for automation accounts (Part 1)
pyotp==2.9.0
